version: 2
updates:
  # Keep packaging, templates and runtime dependencies on manual review.
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
    allow:
      - dependency-name: "ruff"
      - dependency-name: "pytest"
